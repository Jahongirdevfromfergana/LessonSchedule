<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.newlessonchilders">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NewLessonChilders"
        tools:targetApi="31">
        <activity android:name=".screen.SplashActivity"/>
        <activity android:name=".screen.category_item.CategoryItemActivity"/>
        <activity android:name=".screen.DetailActivity"/>
        <activity android:name=".screen.feature_item.FeatureItemActivity"/>
        <activity android:name=".screen.MoreActivity"/>
        <activity android:name=".screen.popular_item.PopularItemActivity"/>
        <activity android:name=".screen.SelectedActivity"/>
        <activity android:name=".screen.ProfileActivity"/>
        <activity
            android:name=".screen.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>